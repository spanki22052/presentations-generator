# API Configuration
# Points to the Docker backend server
# Backend runs on port 3001 (changed from 3000 to avoid conflict with frontend)
# For local development without Docker, use: http://localhost:3001/api
# For Docker backend, use: http://localhost:3001/api
VITE_API_URL=http://localhost:3001/api
