export interface ProfileFormValues {
  firstName: string;
  lastName: string;
  email: string;
  jobTitle: string;
  company: string;
}

export interface ProfileMenuItem {
  id: string;
  label: string;
  icon: string;
  isActive: boolean;
}

export interface ProfilePageProps {}

